<% flash.each do |key, value| %>
  <% next if key == 'timedout' %>
  <div data-alert class="alert-box <%= key %> text-center">
    <%= value -%>
    <a href="#" class="close">&times;</a>
  </div>
<% end %>
